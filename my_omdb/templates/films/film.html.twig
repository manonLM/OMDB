{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
    <div class="row">
        <div class="col-6">
            <img src="{{data.Poster}}" style="width: 100%">
        </div>
        <div class="col ">

            <h1>{{ (data.Title) }} </h1>
            <h4> {{ (data.Year) }} </h4>
            <p>
                {{ (data.Plot) }}
            </p>

        </div>

        </div>
    </div>
{% endblock %}